# @hoite-dev/components

Baseline placeholder package for future shared UI work.

The intent is to use this package for framework-agnostic components later, most likely through web components.

No shared component API, naming convention, styling system, or token system is committed here yet.
