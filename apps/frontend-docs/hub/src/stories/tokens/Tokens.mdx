import { Meta } from '@storybook/addon-docs/blocks';
import { TokensDocRenderer } from './TokensDocRenderer';

<Meta title="Design System/Tokens" />

# Tokens

The shared token source lives in `@hoite-dev/ui` and is emitted as theme-aware CSS custom properties, compiled shared CSS, and typed TypeScript exports.

<TokensDocRenderer />
